version: '3.8'

services:
  ai-orchestrator:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ./ai_cluster_orchestrator/.env
    volumes:
      - ./ai_cluster_orchestrator/src:/app/src
